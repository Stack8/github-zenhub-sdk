query GetRelease($releaseId: ID!,  $endCursor: String) {
    node(id: $releaseId) {
        ... on Release {
            issues(first: 100, after: $endCursor) {
                nodes {
                    id
                    title
                    number
                }
                pageInfo {
                    hasNextPage
                    endCursor
                }
            }
        }
    }
}