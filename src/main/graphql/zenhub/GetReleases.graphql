query GetReleases($workspaceId: ID!) {
    workspace(id: $workspaceId) {
        id
        displayName
        releases {
            nodes {
                title
                state
            }
        }
    }
