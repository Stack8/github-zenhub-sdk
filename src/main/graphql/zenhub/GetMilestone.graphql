query GetMilestone($repositoryGhId: Int!, $milestoneNumber: Int!) {
    milestoneByRepoGhIdAndNumber(milestoneNumber: $milestoneNumber, repositoryGhId: $repositoryGhId) {
        id
        title
        startDate
    }
}