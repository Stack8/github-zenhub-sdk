mutation CreateRelease($createReleaseInput: CreateReleaseInput!) {
    createRelease(input: $createReleaseInput) {
        release {
            id
            title
            startOn
            endOn
        }
    }
}
