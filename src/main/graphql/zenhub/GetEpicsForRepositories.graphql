query GetEpicsForRepositories($workspaceId: ID!, $repositoryGhIds: [Int!]) {
    workspace(id: $workspaceId) {
        epics(repositoryGhIds: $repositoryGhIds) {
            nodes {
                issue {
                    number
                }
            }
        }
    }
}