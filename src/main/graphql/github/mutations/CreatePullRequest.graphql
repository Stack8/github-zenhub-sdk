mutation CreatePullRequest($input: CreatePullRequestInput!) {
    createPullRequest(input: $input) {
        pullRequest {
            id
            number
            state
            url
            repository {
                name
            }
            assignees(first: 5) {
                nodes {
                    login
                }
            }
        }
    }
}
