query GetStatuses($owner: String!, $repo: String!, $commitSha: String!) {
    repository(owner: $owner, name: $repo) {
        object(expression: $commitSha) {
            ... on Commit {
                status {
                    contexts {
                        creator {
                            login
                        }
                        state
                        targetUrl
                    }
                }
            }
        }
    }
}